# Исправление проблемы "Канал: Не указан"

## 🔴 Проблема

После успешного подключения YouTube отображается:
```
✓ Подключено
Канал: Не указан
```

## ✅ Решение

### Шаг 1: Проверьте логи

На сервере выполните:

```bash
sudo tail -50 /var/log/apache2/youpub_error.log | grep -i "youtube\|channel"
```

Ищите строки:
- `YouTube Channel Info Request:`
- `Channel ID:`
- `Channel Name:`

### Шаг 2: Возможные причины

#### Причина 1: API не вернул информацию о канале

Если в логах видно `No channels found in response` или `Failed to get channel info`:

**Решение:** Переподключите YouTube:

1. Откройте: https://you.1tlt.ru/integrations
2. Нажмите **Отключить** (если есть)
3. Нажмите **Подключить YouTube** снова
4. Разрешите доступ к YouTube каналу

#### Причина 2: У аккаунта нет канала YouTube

Если у Google аккаунта нет канала YouTube, API не вернет информацию.

**Решение:** 
1. Создайте канал YouTube для вашего аккаунта
2. Переподключите интеграцию

#### Причина 3: Недостаточно прав доступа

Если в логах видно ошибку 403 или 401:

**Решение:**
1. Отключите интеграцию
2. Подключите снова
3. Убедитесь, что разрешили все запрашиваемые права доступа

### Шаг 3: Обновите код (если нужно)

Если проблема сохраняется, обновите код на сервере:

```bash
cd /ssd/www/youpub
sudo git pull origin main
```

### Шаг 4: Проверьте в базе данных

Проверьте, что информация сохранена:

```bash
mysql -u youpub_user -p youpub_db
```

```sql
SELECT user_id, channel_id, channel_name, status 
FROM youtube_integrations 
WHERE user_id = 1;
```

Если `channel_name` пустой (`NULL`), значит API не вернул название канала.

### Шаг 5: Ручное обновление (если нужно)

Если нужно обновить информацию о канале вручную:

```sql
UPDATE youtube_integrations 
SET channel_name = 'Название канала' 
WHERE user_id = 1;
```

## 🔍 Отладка

### Проверка токена доступа

Если токен валидный, можно проверить вручную:

```bash
# Получите access_token из БД
# Затем выполните:
curl -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  "https://www.googleapis.com/youtube/v3/channels?part=snippet&mine=true"
```

Должен вернуться JSON с информацией о канале.

### Проверка прав доступа

Убедитесь, что в OAuth запросе запрашиваются правильные scopes:
- `https://www.googleapis.com/auth/youtube.upload`
- `https://www.googleapis.com/auth/youtube.readonly`

## 📝 Быстрое решение

1. **Отключите** YouTube интеграцию (если подключена)
2. **Подключите** снова
3. **Разрешите** все запрашиваемые права доступа
4. **Проверьте** логи на наличие ошибок

## ⚠️ Важно

- Убедитесь, что у Google аккаунта есть канал YouTube
- Убедитесь, что разрешили все запрашиваемые права доступа
- Проверьте, что YouTube Data API v3 включен в Google Cloud Console
