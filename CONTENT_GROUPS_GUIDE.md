# Руководство по использованию модуля групп контента

## 📖 Обзор

Модуль групп контента позволяет:
- Группировать видео по темам (например, "Котики", "Мемы", "Релакс")
- Автоматически публиковать видео из групп по расписанию
- Использовать шаблоны для оформления публикаций
- Настраивать гибкие расписания с лимитами и ограничениями

## 🚀 Быстрый старт

### 1. Применение миграции БД

```bash
cd /ssd/www/youpub
mysql -u youpub_user -pqweasd333123 youpub_db < database/migrations/002_content_groups_module.sql
```

### 2. Настройка cron для Smart Worker

```bash
sudo crontab -e
```

Добавить строку:
```
* * * * * /usr/bin/php /ssd/www/youpub/workers/smart_publish_worker.php >> /ssd/www/youpub/storage/logs/smart_publish.log 2>&1
```

### 3. Обновление кода

```bash
cd /ssd/www/youpub
sudo git pull origin main
```

## 📝 Пошаговая инструкция

### Шаг 1: Создание группы

1. Перейдите: `https://you.1tlt.ru/content-groups/create`
2. Заполните:
   - **Название**: например, "Котики"
   - **Описание**: опционально
   - **Шаблон**: выберите шаблон (можно создать позже)
3. Нажмите "Создать"

### Шаг 2: Добавление видео в группу

1. Откройте группу: `https://you.1tlt.ru/content-groups/{id}`
2. Нажмите "Добавить видео"
3. Выберите одно или несколько видео
4. Видео появятся в группе со статусом "new"

### Шаг 3: Создание шаблона (опционально)

1. Перейдите: `https://you.1tlt.ru/content-groups/templates/create`
2. Заполните:
   - **Название**: например, "Шаблон для котиков"
   - **Шаблон названия**: `{title} | {group_name}`
   - **Шаблон описания**: 
     ```
     🎬 {title}
     📁 Группа: {group_name}
     #{group_name} #видео {random_emoji}
     ```
   - **Шаблон тегов**: `{group_name}, видео, {date}`
   - **Emoji список**: `😺,😸,😹,😻,😼,😽` (через запятую)
3. Нажмите "Создать"

### Шаг 4: Создание умного расписания

1. Перейдите: `https://you.1tlt.ru/content-groups/schedules/create`
2. Настройте:
   - **Группа**: выберите созданную группу
   - **Платформа**: YouTube, Telegram или обе
   - **Тип расписания**: 
     - **Fixed** - фиксированное время (каждый день в 12:00)
     - **Interval** - интервальное (каждые 30 минут)
     - **Batch** - пакетное (5 видео за 2 часа)
     - **Random** - случайное (между 10:00 и 18:00)
     - **Wave** - волновое (активные и тихие периоды)
   - **Лимиты**:
     - Дневной лимит: например, 10 видео
     - Часовой лимит: например, 2 видео
   - **Задержка между публикациями**: например, 30 минут
3. Нажмите "Создать"

## 🎨 Переменные в шаблонах

Доступные переменные:
- `{title}` - название видео
- `{group_name}` - название группы
- `{index}` - порядковый номер в группе
- `{date}` - текущая дата (дд.мм.гггг)
- `{platform}` - платформа публикации (youtube, telegram)
- `{random_emoji}` - случайный emoji из списка

### Примеры шаблонов

**Описание для YouTube:**
```
🎬 {title}
📁 Группа: {group_name}
#{group_name} #видео #развлечения
{random_emoji} {random_emoji}
```

**Описание для Telegram:**
```
{title}

#{group_name} #{platform}
{random_emoji}
```

## ⚙️ Типы расписаний

### Fixed (Фиксированное)
- Публикация каждый день в определенное время
- Пример: каждый день в 12:00

**Параметры:**
- `publish_at` - дата и время первой публикации
- `weekdays` - дни недели (1-7, пн-вс)
- `active_hours_start/end` - часы активности

### Interval (Интервальное)
- Публикация каждые N минут/часов
- Пример: каждые 30 минут

**Параметры:**
- `interval_minutes` - интервал в минутах
- `active_hours_start/end` - окно активности

### Batch (Пакетное)
- X видео за Y часов
- Пример: 5 видео за 2 часа

**Параметры:**
- `batch_count` - количество видео
- `batch_window_hours` - окно в часах
- `delay_between_posts` - задержка между публикациями

### Random (Случайное)
- Случайное время в пределах окна
- Пример: между 10:00 и 18:00

**Параметры:**
- `random_window_start` - начало окна
- `random_window_end` - конец окна

### Wave (Волновое)
- Активные и тихие периоды
- Пример: активность 9-12 и 15-18, тишина 12-15

**Параметры:**
- `wave_config` - JSON конфигурация периодов

## 📊 Статусы файлов в группах

- **new** - новый файл, не обработан
- **queued** - в очереди на публикацию
- **published** - успешно опубликован
- **error** - ошибка при публикации
- **skipped** - пропущен

## 🔄 Smart Queue алгоритм

1. Worker находит расписания, готовые к публикации
2. Для каждого расписания:
   - Проверяет лимиты (дневной, часовой)
   - Если есть группа:
     - Берет следующее неопубликованное видео
     - Применяет шаблон (если есть)
     - Публикует
   - Если нет группы (старый режим):
     - Работает как раньше
3. Обновляет статусы файлов в группе
4. Обновляет время следующей публикации (для interval/batch)

## 🛠️ Полезные функции

### Перемешивание группы
Перемешивает порядок видео в группе:
```
POST /content-groups/{id}/shuffle
```

### Массовое добавление видео
Добавляет несколько видео сразу:
```
POST /content-groups/{id}/add-videos
Body: { "video_ids": [1, 2, 3] }
```

### Превью шаблона
Предпросмотр шаблона с тестовыми данными:
```
POST /content-groups/templates/{id}/preview
Body: { "sample_title": "Тест", "sample_group": "Группа" }
```

## ⚠️ Важные замечания

1. **Обратная совместимость**: Старые расписания (без групп) продолжают работать
2. **Лимиты**: Учитываются на уровне пользователя и платформы
3. **Ошибки**: При 5+ ошибках группа автоматически ставится на паузу
4. **Шаблоны**: Применяются только если указаны в расписании или группе

## 🔍 Отладка

### Логи worker'а
```bash
tail -f /ssd/www/youpub/storage/logs/smart_publish_*.log
```

### Проверка статусов
```sql
SELECT * FROM content_groups WHERE user_id = 1;
SELECT * FROM content_group_files WHERE group_id = 1;
SELECT * FROM schedules WHERE content_group_id IS NOT NULL;
```

## 📈 Статистика

Статистика по группам доступна:
- На странице группы: `/content-groups/{id}`
- В общем списке: `/content-groups`

Метрики:
- Всего файлов
- Опубликовано
- В очереди
- Ошибки

## 🚀 Масштабирование

Для больших групп (1000+ видео):
- Используйте индексы БД (уже созданы)
- Настройте кэширование статистики
- Рассмотрите батчинг запросов к API

## 🔐 Безопасность

- Проверка прав доступа к группам
- Валидация шаблонов
- Защита от SQL injection (используются prepared statements)
- CSRF защита для форм
