# Проверка работы страниц после обновления

## ✅ Выполненные действия

1. ✅ Код обновлен через `git pull`
2. ✅ Создана директория для логов: `storage/logs`
3. ✅ Создан файл логов: `storage/logs/error.log`
4. ✅ Установлены права на логи
5. ✅ PHP-FPM перезапущен: `php8.1-fpm`

## 🔍 Проверка работы страниц

### 1. Проверьте страницу шаблонов:
Откройте в браузере: https://you.1tlt.ru/content-groups/templates

Должна открыться страница со списком шаблонов (или пустое сообщение, если шаблонов нет).

### 2. Проверьте страницу умных расписаний:
Откройте в браузере: https://you.1tlt.ru/content-groups/schedules

Должна открыться страница со списком умных расписаний (или пустое сообщение, если расписаний нет).

### 3. Проверьте логи в реальном времени:

```bash
# Откройте страницу в браузере, затем выполните:
tail -f /ssd/www/youpub/storage/logs/error.log
```

Вы должны увидеть логи при загрузке страницы.

### 4. Если страницы все еще не открываются:

```bash
# Проверьте логи PHP-FPM
sudo journalctl -u php8.1-fpm -f

# Проверьте логи веб-сервера
tail -f /var/log/apache2/error.log
# или
tail -f /var/log/nginx/error.log

# Проверьте права на файлы
ls -la /ssd/www/youpub/storage/logs/
ls -la /ssd/www/youpub/views/content_groups/templates/
ls -la /ssd/www/youpub/views/content_groups/schedules/
```

## 🐛 Возможные проблемы и решения

### Проблема: Страница показывает форму входа

**Причина:** Проблема с сессией или авторизацией

**Решение:**
```bash
# Проверьте настройки сессий
php -i | grep session.save_path

# Проверьте права на директорию сессий
ls -la $(php -i | grep "session.save_path" | awk '{print $3}')
```

### Проблема: Ошибка 500 Internal Server Error

**Решение:**
```bash
# Проверьте логи
tail -50 /ssd/www/youpub/storage/logs/error.log

# Проверьте синтаксис PHP файлов
php -l /ssd/www/youpub/app/Modules/ContentGroups/Controllers/TemplateController.php
php -l /ssd/www/youpub/app/Modules/ContentGroups/Controllers/SmartScheduleController.php
```

### Проблема: Страница пустая или показывает JSON

**Причина:** Ошибка в представлении

**Решение:**
```bash
# Проверьте существование файлов представлений
ls -la /ssd/www/youpub/views/content_groups/templates/index.php
ls -la /ssd/www/youpub/views/content_groups/schedules/index.php

# Проверьте права
chmod 644 /ssd/www/youpub/views/content_groups/templates/index.php
chmod 644 /ssd/www/youpub/views/content_groups/schedules/index.php
```

## ✅ Ожидаемый результат

После обновления обе страницы должны:
- ✅ Открываться без ошибок
- ✅ Показывать список шаблонов/расписаний (или сообщение об их отсутствии)
- ✅ Иметь кнопку "Создать" для добавления новых записей
- ✅ Работать без ошибок в консоли браузера
