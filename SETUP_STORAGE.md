# Настройка директории для загрузки видео

## 🔴 Проблема

Ошибка в логах:
```
mkdir(): Permission denied
move_uploaded_file(): Failed to open stream: No such file or directory
```

## ✅ Решение: Создать директорию с правильными правами

### Шаг 1: Создайте директорию storage

Выполните на сервере:

```bash
cd /ssd/www/youpub
sudo mkdir -p storage/uploads
```

### Шаг 2: Установите правильного владельца

```bash
sudo chown -R www-data:www-data storage
```

**Важно:** Замените `www-data` на пользователя, под которым работает PHP-FPM, если он другой.

Чтобы узнать пользователя PHP-FPM:
```bash
ps aux | grep php-fpm | head -1
```

### Шаг 3: Установите права доступа

```bash
sudo chmod -R 755 storage
```

### Шаг 4: Проверьте права

```bash
ls -la /ssd/www/youpub/storage/
```

Должно быть примерно так:
```
drwxr-xr-x www-data www-data storage/
drwxr-xr-x www-data www-data storage/uploads/
```

### Шаг 5: Обновите код на сервере

```bash
cd /ssd/www/youpub
sudo git pull origin main
```

### Шаг 6: Попробуйте загрузить видео снова

1. Откройте: https://you.1tlt.ru/videos/upload
2. Выберите видео файл
3. Нажмите "Загрузить"

## 🔍 Проверка

### Проверьте, что директория создана

```bash
ls -la /ssd/www/youpub/storage/uploads/
```

### Проверьте права на запись

```bash
sudo -u www-data touch /ssd/www/youpub/storage/uploads/test.txt
sudo -u www-data rm /ssd/www/youpub/storage/uploads/test.txt
```

Если команда выполнилась без ошибок - права правильные.

### Проверьте логи после загрузки

```bash
sudo tail -50 /var/log/apache2/youpub_error.log | grep -i "video"
```

Теперь должно быть:
```
Video Upload: File saved successfully to ...
```

## ⚠️ Если проблема сохраняется

### Вариант 1: Проверьте пользователя PHP-FPM

```bash
ps aux | grep php-fpm | head -1
```

Если пользователь не `www-data`, используйте его в команде `chown`:

```bash
sudo chown -R USERNAME:USERNAME /ssd/www/youpub/storage
```

### Вариант 2: Установите права 777 (временно для теста)

```bash
sudo chmod -R 777 /ssd/www/youpub/storage
```

**Внимание:** Это небезопасно для production! Используйте только для тестирования.

После проверки верните права:
```bash
sudo chmod -R 755 /ssd/www/youpub/storage
```

### Вариант 3: Проверьте SELinux (если используется)

```bash
# Проверьте статус SELinux
getenforce

# Если Enforcing, добавьте контекст
sudo chcon -R -t httpd_sys_rw_content_t /ssd/www/youpub/storage
```

## 📝 Быстрая команда (все в одной)

```bash
cd /ssd/www/youpub && \
sudo mkdir -p storage/uploads && \
sudo chown -R www-data:www-data storage && \
sudo chmod -R 755 storage && \
ls -la storage/
```

## ✅ После настройки

После выполнения всех шагов:

1. Директория `storage/uploads` должна существовать
2. Владелец должен быть `www-data` (или пользователь PHP-FPM)
3. Права должны быть `755` для директорий
4. PHP должен иметь возможность создавать поддиректории для пользователей

Теперь загрузка видео должна работать!
